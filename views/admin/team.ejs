<%- include('header.ejs')%>
      <main class="dashboard-content">
        <div class="container-fluid px-3 px-lg-4 py-4">
          <div class="page-heading">
            <div class="page-heading-copy">
              <span class="page-icon"><i class="bi bi-file-earmark" aria-hidden="true"></i></span>
              <div>
                <p class="eyebrow mb-1">Starter</p>
                <h1 class="h3 mb-1">Team</h1>
                
              </div>
            </div>  
          </div>
          <section class="panel"> 
            <form action="/admin/team_save" method="post" enctype="multipart/form-data">
                <label>Name</label>
                <input type="text" class="form-control mb-4" name="name" />
                <label>Position</label>
                <input type="text" class="form-control mb-4" name="position" />
                <button class="btn btn-primary">Save</button>
            </form>

            <table class="table table-border mt-5">
                <tr>
                    <th>Sr.no</th>
                    <th>Name</th>
                    <th>Position</th>
                    <th>Status</th>
                </tr>
                <% for(var i=0;i<data.length;i++){ %>
                    <tr>
                    <th><%= i+1; %></th>
                    <th><%=data[i].name; %></th>
                    <th><%=data[i].position; %></th>
                    <th>
                        <a href="/admin/team_edit/<%=data[i].tid;%>"><button class="btn ">Edit</button></a>
                        <a href="/admin/team_delete/<%=data[i].tid;%>"><button class="btn ">Delete</button></a>
                    </th>
                </tr>

                <% } %>
            </table>
           
          </section>
        </div>
      </main>

      <%- include('footer.ejs')%>